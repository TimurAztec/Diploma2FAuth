function Header(props: any) {

    return (
        <header className={`w-full flex fixed bg-gradient-to-r from-white to-primary shadow-3xl`}>
            <section className={`w-full flex flex-col md:flex-row items-center p-4`}>
                
            </section>
        </header>
    )
}

export {Header};